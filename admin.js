const key="votrem_local_cms";let data=JSON.parse(localStorage.getItem(key)||'{"media":[]}');function save(){localStorage.setItem(key,JSON.stringify(data));render()}function render(){list.innerHTML=data.media.map((x,i)=>`<div><b>${x.title}</b> — ${x.description||""} <button onclick="data.media.splice(${i},1);save()">Remove</button></div>`).join("")}add.onclick=()=>{if(!title.value||!image.value)return alert("Add a title and image path.");data.media.push({title:title.value,description:description.value,image:image.value,url:url.value});title.value=description.value=image.value=url.value="";save()};render();